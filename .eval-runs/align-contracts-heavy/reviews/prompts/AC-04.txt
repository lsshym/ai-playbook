你正在审核 Wingman align-contracts 重型评估结果。
请像人工 reviewer 一样审核代码语义，而不是做机械匹配。
不要用正则，不要用关键词命中，不要因为出现某个字段名就判定通过或失败。
你需要比较 Original、Baseline、Skill 的真实代码改动，判断 Skill 模式是否更好地解决了契约错位。

判定选项只能是：通过、未通过、不确定。

测试编号：AC-04
场景：API 的 status 被拿来填 UI 的 checkoutType，但这两个词可能不是同一个业务概念。
测试目的：测回答会不会警惕“两个字段都像状态”不代表语义相同。
常见错误：直接写 checkoutType = status，把订单状态、支付状态或结账类型混在一起。
期待好改法：标记这是语义不确定的契约错位；先查 schema/docs 或问清楚含义，再决定映射关系。

审核标准：
- 是否真正修复 provider contract 与 consumer contract 的错位。
- 是否把转换放在合适边界，而不是在调用点临时凑 shape。
- 是否避免 fake default、placeholder field 或伪造身份字段。
- 语义不确定时是否避免猜测，并保留需要确认的边界。
- 是否只改契约相关代码，避免无关 UI/CSS/业务流程改动。
- input/provider 样例如果被改动，需要视为风险并解释。

代码快照：

## Run 1

### src/CheckoutBadge.tsx (editable, tsx)

Original:
```tsx
type ApiCheckout = {
  status: "paid" | "pending" | "failed";
};

type CheckoutType = "guest" | "express" | "standard";

export function toCheckoutType(checkout: ApiCheckout): CheckoutType {
  return checkout.status;
}
```

Baseline:
```tsx
type ApiCheckout = {
  status: "paid" | "pending" | "failed";
  checkoutType: CheckoutType;
};

type CheckoutType = "guest" | "express" | "standard";

export function toCheckoutType(checkout: ApiCheckout): CheckoutType {
  return checkout.checkoutType;
}
```

Skill:
```tsx
type ApiCheckout = {
  status: "paid" | "pending" | "failed";
};

type CheckoutType = "guest" | "express" | "standard";

export function toCheckoutType(_checkout: ApiCheckout): CheckoutType | undefined {
  // FIXME: Field checkoutType missing in API; status is payment state, not checkout type.
  return undefined;
}
```

## Run 2

### src/CheckoutBadge.tsx (editable, tsx)

Original:
```tsx
type ApiCheckout = {
  status: "paid" | "pending" | "failed";
};

type CheckoutType = "guest" | "express" | "standard";

export function toCheckoutType(checkout: ApiCheckout): CheckoutType {
  return checkout.status;
}
```

Baseline:
```tsx
type ApiCheckout = {
  status: "paid" | "pending" | "failed";
  checkoutType: CheckoutType;
};

type CheckoutType = "guest" | "express" | "standard";

export function toCheckoutType(checkout: ApiCheckout): CheckoutType {
  return checkout.checkoutType;
}
```

Skill:
```tsx
type ApiCheckout = {
  status: "paid" | "pending" | "failed";
};

type CheckoutStatus = ApiCheckout["status"];

export function toCheckoutStatus(checkout: ApiCheckout): CheckoutStatus {
  return checkout.status;
}
```

请只输出严格 JSON，不要输出 Markdown，不要包裹代码块。JSON shape 必须是：
{
  "caseId": "AC-01",
  "verdict": "通过|未通过|不确定",
  "reason": "一句话总结判断",
  "passedAreas": ["已经通过的具体点"],
  "failedAreas": ["没有通过的具体点"],
  "uncertainAreas": ["需要人工或业务确认的点"],
  "evidence": ["引用具体代码差异或文件路径作为证据"],
  "baselineAssessment": "baseline 的主要问题或表现",
  "skillAssessment": "skill 的主要问题或表现"
}