# Scenario: Explicit React TypeScript Refactor

Use /react-ts-refactor to analyze this oversized React + TypeScript component. Include props, inline interfaces, duplicated type shapes, and component logic organization in one diagnostic table. Wait for approval before making code changes.
